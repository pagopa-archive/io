/**
 * Configuration file for API client examples.
 */
'use strict'

const config = {
  // This is the notification API endpoint
  // The default URL points to localhost assuming you've already
  // started the mock HTTP API server with the command `npm run serve`
  NOTIFICATION_API_URL: 'http://localhost:10010/api/v1'
}

module.exports = config
